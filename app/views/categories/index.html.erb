<h1>Categorias</h1>

<table>
  <tr>
    <th>Categoria</th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= link_to category.categoria, category %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('label.new'), new_category_path %>
