<h1>Listing bills</h1>

<table>
  <tr>
    <th>Barcode</th>
    <th>Date</th>
    <th>Amount</th>
    <th>Bill kind</th>
    <th>Client</th>
    <th>Items</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bills.each do |bill| %>
  <tr>
    <td><%= bill.barcode %></td>
    <td><%= bill.date %></td>
    <td><%= bill.amount %></td>
    <td><%= bill.bill_kind %></td>
    <td><%= bill.client_id %></td>
    <td><%= bill.items %></td>
    <td><%= link_to 'Show', bill %></td>
    <td><%= link_to 'Edit', edit_bill_path(bill) %></td>
    <td><%= link_to 'Destroy', bill, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bill', new_bill_path %>
