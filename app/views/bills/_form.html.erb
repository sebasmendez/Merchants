<%= form_for(@bill) do |f| %>
  <% if @bill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill.errors.count, "error") %> prohibited this bill from being saved:</h2>

      <ul>
      <% @bill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :barcode %>
    <%= @bill.barcode %>
    <%= f.hidden_field :barcode %>
  </div>
  <div class="field">
    <%= f.label :date %>  <%= f.hidden_field :date, value: Date.today %><%= l Date.today %>
  </div>
 
  <div class="field">
    <%= f.label :client %>
    <%= f.select :client_id, Client.all.map {
  |r| [r.document, r.id]}, :include_blank => true %>
    <%= f.label :bill_kind %>
    <%= f.select :bill_kind, bill_for_select %>
  </div>
  <div class="field">
    <%= f.label :items %><br />
    <%= f.text_area :items %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    $<%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
