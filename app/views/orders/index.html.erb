<h1> <%= t('titles.order') %></h1>

<table>
  <tr>
    <th><%= Client.human_attribute_name(:client) %></th>
    <th>Día</th>
    <th>Precio</th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= link_to (order.client ? order.client.name.split.first + ' ' + order.client.last_name.split.first : 
 t('others.without_client')), order%></td>
  <td> <%= l order.created_at, format: :little %> </td>
  <td> <%= number_to_currency(order.price) %> </td>
  </tr>
<% end %>
</table>

<br />

<%= will_paginate @orders %>


<%= link_to "Excel con todas", orders_path(format: "csv") %>
<%= link_to "Excel con mes", '#', class: 'excel_with_month' %>
