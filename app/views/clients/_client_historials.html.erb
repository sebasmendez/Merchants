<% if @client_orders.present? %>
  <table>
    <tr><th colspan="4"> Órdenes </th></tr>
    <tr>
      <th>Orden</th>
      <th>Fecha</th>
      <th>Monto</th>
      <th>Lo Pago?</th>
    </tr>
    <% @client_orders.each do |order| %>
      <tr>
        <td><%= link_to order.id, order_path(order.id) %></td>
        <td><%= l order.created_at, format: :little %></td>
        <td><%= number_to_currency order.price %></td>
        <td><%= order.to_amount ? 'No' : 'Si' %></td>
      </tr>
    <% end %>
    <% if @client_orders.count > 5 %>
      <tr>
        <td colspan="4">
          <%= will_paginate @client_orders, param_name: 'order_page' %>
        </td>
      </tr>
    <% end %>
  </table>
<% end %>
